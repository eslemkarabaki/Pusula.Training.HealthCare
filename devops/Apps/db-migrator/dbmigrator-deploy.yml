version: "3.9"
services:
  db-migrator:
    image: registry.gitlab.com/berkmkaracayir/pusula:${imageTag}
    environment:
      - ConnectionStrings__Default=Host=64.226.68.43;Port=5432;Database=HealthCare;User ID=postgres;Password=myPassw0rd;
      - ASPNETCORE_ENVIRONMENT=Production
    depends_on:
      - postgres
    networks:
      - frontend
    deploy:
      restart_policy:
        condition: on-failure
      placement:
        constraints: [node.role==manager]

networks:
  frontend:
    external: true
